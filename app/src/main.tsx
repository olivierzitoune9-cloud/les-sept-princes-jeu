import React from 'react'
import ReactDOM from 'react-dom/client'
import AppVolley from './components/AppVolley'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppVolley />
  </React.StrictMode>,
)
